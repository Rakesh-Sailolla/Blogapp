<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>All Posts</title>

  <link
    href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css"
    rel="stylesheet"
  >

  <style>
    body { font-family: Arial, sans-serif; padding: 20px; }
    .post {
      border: 1px solid #ccc;
      padding: 15px;
      margin-bottom: 20px;
      border-radius: 8px;
    }
    img {
      max-width: 100%;
      border-radius: 5px;
      margin-top: 10px;
    }
    a { text-decoration: none; color: inherit; }
  </style>
</head>

<body>
  <%- include("includes/navbar") %>

  <h1>All Posts</h1>
  <a href="/posts/new" class="btn btn-primary mb-3">Create New Blog</a>

  <% if (posts.length === 0) { %>
    <p>No posts yet.</p>
  <% } else { %>

    <% posts.forEach(post => { %>
      <div class="post">

        <!-- Only title is clickable -->
        <a href="/posts/<%= post._id %>">
          <h2><%= post.title %></h2>
        </a>

        <p><%= post.content %></p>

        <p>
          Author:
          <%= post.author?.username || "Unknown" %>
        </p>

        <p><em>Type: <%= post.type %></em></p>

        <p>
          Date:
          <%= post.createdAt ? new Date(post.createdAt).toDateString() : "N/A" %>
        </p>

        <!-- Image OUTSIDE anchor -->
        <% if (post.image && post.image.url) { %>
          <img src="<%= post.image.url %>" alt="blog image">
        <% } %>

      </div>
    <% }) %>

  <% } %>

  <%- include("includes/footer") %>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
